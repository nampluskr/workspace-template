# 워크스페이스 가이드

이 파일은 AI CLI 가 워크스페이스에서 작업할 때 따르는 운영 지침이다.
프로젝트 내용 및 진행 사항은 PROJECT.md 와 TASKS.md 를 참조한다.

다른 AI CLI 사용 시 이 파일을 복사하여 해당 CLI 의 지침 파일명으로 rename 한다.
- Claude: CLAUDE.md
- Codex: AGENTS.md
- Gemini: GEMINI.md
- RooCode: ROO.md

> 생성일시: 260605-195326
> 수정일시: 260607-000000
> 주제: Development Environment and Tools

**목차**

- [워크스페이스 가이드](#워크스페이스-가이드)
  - [1. 용어 정의](#1-용어-정의)
  - [2. 문서 작성 규칙](#2-문서-작성-규칙)
  - [3. AI CLI 응답 스타일](#3-ai-cli-응답-스타일)
  - [4. 프로젝트 구조](#4-프로젝트-구조)
  - [5. 폴더 규칙](#5-폴더-규칙)
    - [5.1. 접두사 규칙](#51-접두사-규칙)
    - [5.2. 폴더 역할](#52-폴더-역할)
    - [5.3. 데이터 흐름](#53-데이터-흐름)
  - [6. 네이밍 규칙](#6-네이밍-규칙)
  - [7. 코딩 컨벤션](#7-코딩-컨벤션)
  - [8. 작업 방식](#8-작업-방식)
  - [9. Jupyter Book v2](#9-jupyter-book-v2)
  - [10. 제한 사항](#10-제한-사항)
  - [11. 커스텀 명령어](#11-커스텀-명령어)

## 1. 용어 정의

이 문서에서 사용하는 용어의 기준이다.
사용자는 두 용어를 혼용할 수 있으며, 문맥에 따라 같은 대상을 가리킨다.

| 용어 | 의미 | 주로 사용되는 맥락 |
|------|------|-----------------|
| 워크스페이스 | 작업 환경/공간 (폴더 구조, 운영 규칙, AI CLI 설정) | 폴더 구조, CLAUDE.md, `_workspace/` |
| 프로젝트 | 수행하는 작업 내용 (목적, 범위, 단계, 산출물) | PROJECT.md, TASKS.md, 작업 명세 |

## 2. 문서 작성 규칙

문서 작성은 이 워크스페이스에서 수행하는 모든 작업의 핵심이다. 아래 규칙은 절대 원칙으로, 이 워크스페이스에서 생성하거나 수정하는 모든 문서에 예외 없이 적용한다.

- 서술체(`~이다`, `~한다`) 사용 — 경어체(`~입니다`, `~합니다`)는 문서 본문에 사용 불가
- 이모지 사용 절대 금지
- 경어체, 불필요한 볼드·이탤릭 강조 남용 금지
- 기술 문서 또는 학술 문서 형식 — 구어체, 비격식 표현, 감탄사 사용 불가
- 항목은 반드시 개조식으로 작성하며, 핵심 키워드를 중심으로 의미와 맥락이 명확히 전달되도록 기술하고 단순 명사 나열이나 지나치게 압축된 표현 금지
- 항목명이 필요한 경우에만 `항목명: 개조식 설명` 형태로 사용
- 테이블·숫자/불릿 목록 바로 앞에 도입 문장 한 줄 이상 삽입 — 나열 항목의 맥락과 존재 이유를 먼저 제시
- 세부 규칙 참조: `_workspace/rules/writing-style.md`

## 3. AI CLI 응답 스타일

AI CLI 가 사용자에게 응답할 때 다음 스타일 규칙을 따른다.
이 규칙은 §2 문서 작성 규칙과 별개로 적용되며, AI CLI 의 대화 응답에만 해당한다.

- `~합니다`, `~입니다` 경어체 응답 — 서술체(`~이다`, `~한다`)는 문서 본문 전용
- 응답 텍스트에 이모지 사용 불가
- 불필요한 사과, 서두, 재확인 없이 요청 즉시 처리

## 4. 프로젝트 구조

프로젝트 루트의 기본 디렉토리 구조는 다음과 같다. 각 폴더와 파일의 역할은 §5 폴더 규칙에서 상세히 정의한다.

```
project-root/
├── README.md                   # 프로젝트 외부 소개
├── PROJECT.md                  # 프로젝트 내부 명세
├── TASKS.md                    # Stage-Phase-Task 진행 관리
├── CLAUDE.md                   # AI CLI 워크스페이스 운영 지침 (이 파일)
├── AGENTS.md                   # Codex 용 AI CLI 워크스페이스 운영 지침
├── _workspace/                 # 워크스페이스 운영 관리
│   ├── commands/               # 커스텀 명령어 프롬프트
│   ├── docs/                   # 워크스페이스 사용법 및 운영 문서
│   ├── rules/                  # 워크스페이스 공통 규칙
│   └── sessions/               # 세션 핸드오프 문서
├── _assets/                    # 읽기 전용 입력 보관소
├── scripts/                    # 실행 스크립트 및 Python 파일
├── outputs/                    # 프로젝트 산출물
└── docs/                       # 최종 산출물 (Jupyter Book v2)
    ├── myst.yml
    └── contents/
        └── chap01/
            └── images/
```

## 5. 폴더 규칙

### 5.1. 접두사 규칙

폴더명 접두사로 내부 운영 폴더와 프로젝트 산출물 폴더를 구분한다. 접두사 규칙은 다음과 같다.

| 접두사 | 의미 | 예시 |
|--------|------|------|
| `_` | 내부 운영/보조 폴더 (프로젝트 산출물 아님) | `_workspace/`, `_assets/` |
| 없음 | 프로젝트 산출물 또는 표준 폴더 | `docs/`, `scripts/`, `outputs/` |

### 5.2. 폴더 역할

각 폴더의 역할과 사용 기준은 다음과 같다. `_` 접두사 폴더는 운영 보조용이며 프로젝트 산출물로 취급하지 않는다.

| 폴더 | 역할 | 비고 |
|------|------|------|
| `_workspace/` | 워크스페이스 운영 문서 및 세션 기록 | |
| `_workspace/commands/` | 커스텀 명령어 정의 파일 | `{파일명} 실행` 또는 `@{파일명.md} 실행` 형식으로 호출 |
| `_workspace/rules/` | 워크스페이스 공통 규칙 파일 | 문서 작성 스타일 등 |
| `_assets/` | 읽기 전용 입력 보관소 | 참조만 가능, 수정/가공 금지 |
| `scripts/` | 실행 스크립트 및 Python 파일 | 문서 생성 스크립트 포함 |
| `outputs/` | 프로젝트 산출물 | 하위 구조는 프로젝트별 추가 |
| `docs/` | Jupyter Book v2 배포 가능한 최종 산출물 | |

### 5.3. 데이터 흐름

입력 자료에서 최종 문서까지의 처리 흐름은 다음과 같다. `_assets/` 는 원본 보관소이므로 이 흐름에서 수정 없이 읽기만 한다.

```
_assets/          →      scripts/       →      outputs/
(읽기 전용 입력)           (처리/실행)           (중간/최종 산출물)
                                                    ↓
                                             docs/contents/
                                             (최종 문서)
```

## 6. 네이밍 규칙

파일과 폴더 이름은 다음 규칙을 따른다. 일관된 네이밍은 탐색과 자동화 스크립트 적용을 쉽게 한다.

- 폴더명: 소문자, 하이픈 구분 (예: `chap01`, `session-handoff`)
- 루트 레벨 MD 파일: 대문자 (예: `README.md`, `CLAUDE.md`)
- Python 파일: 소문자, 언더스코어 구분 (예: `train_model.py`)
- 챕터 폴더: `chap01`, `chap02`, ... (두 자리 숫자, 0 패딩)
- 세션 핸드오프 파일: `YYMMDD-HHMMSS_session-handoff.md`

## 7. 코딩 컨벤션

이 워크스페이스의 모든 코드에 다음 컨벤션을 준수한다. 언어별 예외가 필요한 경우 해당 스크립트 상단에 명시한다.

- 언어: Python 우선
- 경로 표기: `os.path` 방식 사용 (`pathlib.Path` 사용 금지)
- 코드 내 주석: 영어로 작성
- 들여쓰기: 스페이스 4칸
- 인코딩: UTF-8

## 8. 작업 방식

세션 시작부터 종료까지 다음 순서와 원칙을 따른다. 각 단계는 프로젝트 문서의 일관성과 추적 가능성을 유지하기 위한 것이다.

1. 작업 시작 전 `PROJECT.md`, `TASKS.md` 읽기 — 목적·범위·진행 상황 파악
2. 작업 완료 후 `TASKS.md` 해당 Task 체크박스 완료 상태로 업데이트
3. `_assets/` 파일: 참조 전용 — 수정·가공·삭제 금지
4. 생성된 중간·최종 산출물을 `outputs/` 에 저장
5. 문서 포함 이미지는 `docs/contents/chapXX/images/` 에 저장

## 9. Jupyter Book v2

최종 문서는 Jupyter Book v2 형식으로 `docs/` 에 관리한다. 빌드는 사용자가 직접 실행하며, AI CLI 는 빌드 결과물을 직접 생성하지 않는다.

- 설정 파일 위치: `docs/myst.yml`
- 문서 내용 위치: `docs/contents/chapXX/`
- 문서에 포함되는 이미지: `docs/contents/chapXX/images/`
- 빌드 결과물: `docs/_build/` — `.gitignore` 등록, AI CLI 직접 생성 금지

## 10. 제한 사항

워크스페이스 무결성 유지와 사용자 작업 보호를 위해 다음 사항을 반드시 준수한다.

- `_assets/` 내 파일 수정 및 삭제 금지
- 루트 레벨 필수 파일 (`README.md`, `PROJECT.md`, `TASKS.md`, `CLAUDE.md`) 임의 삭제 금지
- `docs/_build/` 직접 생성 금지 — 빌드는 사용자가 직접 실행
- git commit, push 수행 금지 — 사용자가 VSCode UI 로 직접 수행

## 11. 커스텀 명령어

커스텀 명령어는 `_workspace/commands/` 에 저장된 Markdown 파일로 정의된다.
아래 두 가지 방법으로 호출하면, 해당 파일의 내용을 프롬프트로 실행한다.

- 방법 1: `{파일명 (확장자 제외)} 실행`
- 방법 2: `@{파일명.md} 실행`
- 정의 파일 위치: `_workspace/commands/`
- 명령어 추가: `_workspace/commands/` 에 Markdown 파일 추가 시 즉시 사용 가능

호출 예시:

```
project-init 실행
@project-init.md 실행
```

현재 등록된 커스텀 명령어 목록은 `_workspace/commands/` 폴더를 확인한다.
명령어별 상세 설명은 `_workspace/docs/workspace-guide.md` §5 를 참조한다.

> 다른 AI CLI 사용 시에도 동일하게 적용된다.
> `_workspace/commands/` 폴더의 파일명과 내용을 참조하여 동일한 방식으로 명령어를 처리한다.
